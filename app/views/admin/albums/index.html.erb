<% render partial: "/admin/shared/bootstrap_table", 
	locals: {
		columns: @columns,
		rows: @albums.take(2),
		page: @current_page,
		per_page: @per_page,
		count: @albums.count
	}
%>

<%= render partial: "/admin/shared/bootstrap_table_alt", 
	locals: {
		resource_name: @resource_name,
		columns: @columns,
		rows: @albums,
		page: @current_page,
		per_page: @per_page,
		count: @albums.count
	}
%>